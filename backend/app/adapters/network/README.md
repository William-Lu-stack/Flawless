# Network Adapter 接入

本目录由网络平台团队维护。每个产品或控制器一个模块，统一输出 `network` 域资源、证据和恢复验证结果。

至少覆盖：拓扑、设备与接口状态、链路丢包/时延、路由、DNS、负载均衡、ACL/安全策略和最近变更。Adapter 只读；真实变更必须映射为类型化动作，经风险与爆炸半径评估、人工审批、受控执行和重新取证验证。

禁止接收任意设备 CLI。凭据只能使用 Secret 引用或工作负载身份，不能进入资源事实、日志、事件或回执。
