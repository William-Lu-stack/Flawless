# 企业存储 Adapter 接入

本目录由存储平台团队维护。一个存储产品一个模块，统一输出 `storage` 域资源。

最低证据合同见 `spec.py`：系统健康、存储池容量、卷状态、路径、时延/IOPS、ACL、快照、复制和最近变更。验证应同时检查存储端与消费端；例如扩容成功不仅要看到阵列容量变化，还要验证主机或 Pod 中的文件系统容量。

推荐模块：`oceanstor.py`、`dorado.py`、`ceph.py`、`nfs.py`、`san.py`、`csi_bridge.py`。高风险删除、快照恢复、主备切换必须二次确认。
